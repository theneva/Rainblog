<% if current_user == @post.author %>
	<h1>Editing post</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @post %> |
	<%= link_to 'Back', posts_path %>
<% else %>
	<h1>You have to be logged in to that profile if you want to do that</h1>
	<p>This should be done with a before_filter, but it wouldn't work properly. This \is\ a workaround.</p>
<% end %>
