<%= form_for([@post, @post.comments.build]) do |f| %>

  	<div class="field">
	    <%= f.label 'Write a comment' %><br />
    	<%= f.text_area :content %>
  	</div>
  	<div class="actions">
	    <%= f.submit 'Post comment' %>
  	</div>
<% end %>